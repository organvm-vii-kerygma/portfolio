<section class="bento-cell bento-cell--hero bento-cell--span-2" aria-labelledby="hero-title">
	<p class="hero__eyebrow">Production-systems architect</p>
	<h1 id="hero-title">I build production systems that solve expensive problems.</h1>
	<p class="hero__thesis">I design the operating system around complex software delivery: decision rights, work routing, verification, cost boundaries, evidence, and handoff.</p>
	<p class="hero__availability">Available for senior systems roles and bounded client engagements.</p>
	<p class="hero__authorship">Architected and directed by one person through a governed multi-agent production system.</p>
</section>

<style>
	.hero__eyebrow { color: var(--organ-text-safe); font: 0.72rem/1.4 var(--font-mono); letter-spacing: 0.12em; text-transform: uppercase; }
	h1 { font: clamp(2.2rem, 5.6vw, 4.8rem)/0.98 var(--font-heading); letter-spacing: -0.045em; max-width: 12ch; margin: var(--space-lg) 0; }
	.hero__thesis { max-width: 62ch; color: var(--text-secondary); font-size: clamp(1rem, 1.4vw, 1.18rem); line-height: 1.65; }
	.hero__availability { color: var(--success-text); font-weight: 700; margin-top: var(--space-xl); }
	.hero__authorship { color: var(--text-muted); font: 0.76rem/1.5 var(--font-mono); max-width: 70ch; margin-top: var(--space-sm); }
</style>
